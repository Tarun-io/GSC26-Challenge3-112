# Phase 1-2: reading + querying the raw Parquet dataset
duckdb>=0.10
pandas>=2.0
pyarrow>=14.0
pyyaml>=6.0

# Phase 3: feature validation / correlation study
scipy>=1.11
scikit-learn>=1.3

# Phase 5: per-cluster model training
xgboost>=2.0
lightgbm>=4.0